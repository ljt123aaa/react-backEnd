import { Watermark as WatermarkComponent } from 'antd';

export default function Watermark() {
    return (
        <WatermarkComponent content={'Watermark'} className='w-full h-full flex items-center justify-center'>
        </WatermarkComponent>
    );
}